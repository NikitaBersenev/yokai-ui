class PAGES_SITE {
    private root = ''

    HOME = this.root;
    ARTICLES = this.root + '/article';
    PROJECTS = this.root + '/project';
    CONTACTS = this.root + '/contacts';
    SETTINGS = this.root + '/settings';
}

export const PAGES = new PAGES_SITE();